{
  "compilerOptions": {
    "target": "es2020",                        /* Especifica la versión de ECMAScript que se debe utilizar. */
    "module": "commonjs",                      /* Especifica el sistema de módulos que se debe utilizar. */
    "strict": true,                            /* Habilita todas las opciones de TypeScript estrictas. */
    "esModuleInterop": true,                   /* Mejora la interoperabilidad entre los CommonJS y los módulos ES6 (ESM). */
    "skipLibCheck": true,                      /* Evita la comprobación de las definiciones de tipos de las librerías. */
    "forceConsistentCasingInFileNames": true,   /* Asegura que los nombres de archivo coincidan con la capitalización exacta. */
    "outDir": "./dist",                        /* Directorio de salida para los archivos compilados. */
    "sourceMap": true                          /* Genera archivos de mapas de origen (.map) para facilitar la depuración. */
  },
  "include": [
    "src/**/*.ts"                             /* Archivos TypeScript que se incluirán en la compilación. */
  ],
  "exclude": [
    "node_modules"                            /* Carpetas y archivos que se deben excluir de la compilación. */
  ]
}

